# MEMORY — Chỉ mục trí nhớ

- [Phương pháp dựng câu từ giọng Việt của em](method-mirror-vietnamese-voice.md) — em đưa cách nói tiếng Việt hằng ngày → thầy dựng bộ tiếng Anh khớp giọng đó
- [Quy tắc generate: chỉ thêm mới, không trùng, không sửa cũ](generate-rules-append-only.md) — mỗi lần "generate" chỉ xử lý phần mới & append
- [Không build khi dev đang chạy](dev-never-build-while-dev-running.md) — dùng `tsc --noEmit` để kiểm tra; build/rm .next khi dev chạy gây lỗi module giả
